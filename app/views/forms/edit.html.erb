<%= render 'shared/navbar_org' %>

<div class="container mt-4">
  <div class="row">
    <div class="col-12">
      <div class="d-flex justify-content-between align-items-center mb-4">
        <h1>Edit Form</h1>
        <%= link_to "Back to Forms", organization_forms_path(@organization), class: "btn btn-secondary" %>
      </div>

      <%= render 'form', form: @form %>
    </div>
  </div>
</div>
