import { Controller } from "@hotwired/stimulus"

export default class extends Controller {
  connect() {
    console.log("Organization tasks index controller connected")
  }

  // Add any interactive functionality here as needed
} 